defmodule LearningKeywordLists do
  def create_keyword_list do
    # Are the associative collections in Elixir
    # A keyword list is a special list of two element tuples
    # First element is an atom
    [foo: "bar", hello: "world"]

    # Keys are atoms
    # Keys are ordered
    # Keys don't need to be unique
  end
end
